<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <%= link_to user.name, user_path(user) %>
        <!--<%= render 'relationships/follow_button', user: @user %>-->
      </li>
    <% end %>
  </ul>
  <%== pagy_nav(@pagy) %>
<% end %>
